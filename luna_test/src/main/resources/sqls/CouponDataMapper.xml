<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.luna.coupont">

	<select id="getAllList" resultType="CouponDTO">
		SELECT CSEQ, CONTENT, DISCOUNT, ID 
		FROM COUPON		
	</select>
	
	<insert id="insertCoupon" parameterType="CoupontDTO">
		INSERT INTO COUPON (CSEQ, CONTENT, DISCOUNT, ID)
		VALUES(COUPON_SEQ.NEXTVAL, #{content}, #{discount}, null)		
	</insert>
	
	<select id ="getCoupon" resultType="CouponDTO" parameterType="int">
		SELECT CSEQ, CONTENT, DISCOUNT, ID
		FROM COUPON
		WHERE cseq = #{cseq}
 	</select>
	
	<update id ="updateCoupon" parameterType="CouponDTO">
		UPDATE COUPON
		SET CONTENT=#{content}, DISCOUNT=#{discount}
		WHERE CSEQ=#{cseq}
	</update>
	
	<delete id ="deleteCoupon" parameterType="int">
		DELETE FROM COUPON
		WHERE CSEQ =#{cseq}
	</delete>
	
	<delete id="mulDel" parameterType="Map">
		DELETE FROM COUPON WHERE CSEQ IN  
		<foreach collection="chks" item="i" separator="," open="(" close=")">
			#{i}
		</foreach>
	</delete>
</mapper>